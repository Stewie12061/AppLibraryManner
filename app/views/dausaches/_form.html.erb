<div class="container-fluid">
  <%= form_with(model: dausach) do |form| %>
    <% if dausach.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(dausach.errors.count, "error") %> prohibited this dausach from being saved:</h2>

        <ul>
          <% dausach.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field form-group">
      <%= form.label "Đầu sách" %>
      <%= form.text_field :tensach, class:"form-control" %>
    </div>

    <div class="field form-group">
      <%= form.label "Thể loại" %>
      <%= form.text_field :theloai, class:"form-control" %>
    </div>

    <div class="field form-group">
      <%= form.label "Tác giả" %>
      <%= form.text_field :tacgia, class:"form-control" %>
    </div>

    <div class="actions">
      <%= form.submit "Lưu chỉnh sửa", class:"btn btn-color" %>
    </div>
  <% end %>
</div>